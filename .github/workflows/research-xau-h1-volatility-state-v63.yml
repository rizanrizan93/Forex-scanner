name: XAU H1 Volatility State V63

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "src/fx_scanner/research_xau_h1_volatility_state_v63.py"
      - "src/fx_scanner/research_xau_h1_volatility_state_v63_runtime.py"
      - "tests/test_research_xau_h1_volatility_state_v63.py"
      - ".github/workflows/research-xau-h1-volatility-state-v63.yml"

permissions:
  contents: read

jobs:
  era:
    strategy:
      fail-fast: false
      matrix:
        era: ["2012_2018", "2019_2024", "2025_2026YTD"]
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      V63_ERA: ${{ matrix.era }}
      V63_EVIDENCE_OUTPUT: artifacts/xau-h1-volatility-state-v63-${{ matrix.era }}.json
      SUPABASE_URL: https://fotzcxjeypmjldhvfskt.supabase.co
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
      - name: Install research runtime
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-research-cloud.txt
          pip install pytest==9.0.2 dukascopy-python==4.0.1
      - name: Unit tests
        run: pytest -q tests/test_research_xau_h1_volatility_state_v63.py
      - name: Run H1 volatility-state study
        run: python -m fx_scanner.research_xau_h1_volatility_state_v63_runtime
      - name: Upload evidence
        uses: actions/upload-artifact@v4
        with:
          name: xau-h1-volatility-state-v63-${{ matrix.era }}
          path: artifacts/xau-h1-volatility-state-v63-${{ matrix.era }}.json
          if-no-files-found: error
          retention-days: 30
