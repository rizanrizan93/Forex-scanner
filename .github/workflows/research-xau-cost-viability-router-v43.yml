name: XAU Cost Viability Router V43

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "src/fx_scanner/research_xau_cost_viability_router_v43.py"
      - "src/fx_scanner/research_xau_cost_viability_router_v43_runtime.py"
      - "tests/test_research_xau_cost_viability_router_v43.py"
      - ".github/workflows/research-xau-cost-viability-router-v43.yml"

permissions:
  contents: read

jobs:
  era:
    strategy:
      fail-fast: false
      matrix:
        era: ["2012_2018", "2019_2024", "2025_2026YTD"]
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      V43_ERA: ${{ matrix.era }}
      V43_EVIDENCE_OUTPUT: artifacts/xau-cost-viability-router-v43-${{ matrix.era }}.json
      SUPABASE_URL: https://fotzcxjeypmjldhvfskt.supabase.co
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
      - name: Install research runtime
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-research-cloud.txt
          pip install pytest==9.0.2 dukascopy-python==4.0.1
      - name: Unit tests
        run: pytest -q tests/test_research_xau_cost_viability_router_v43.py
      - name: Run cost viability era study
        run: python -m fx_scanner.research_xau_cost_viability_router_v43_runtime
      - name: Upload evidence
        uses: actions/upload-artifact@v4
        with:
          name: xau-cost-viability-router-v43-${{ matrix.era }}
          path: artifacts/xau-cost-viability-router-v43-${{ matrix.era }}.json
          if-no-files-found: error
          retention-days: 30
