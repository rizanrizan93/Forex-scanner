name: XAU AFIC External Liquidity V153 Recent
on:
  workflow_dispatch:
  pull_request:
    paths:
      - "src/fx_scanner/research_xau_afic_external_liquidity_v153.py"
      - "src/fx_scanner/research_xau_afic_external_liquidity_v153_recent_runtime.py"
      - "tests/test_research_xau_afic_external_liquidity_v153.py"
      - ".github/workflows/research-xau-afic-external-liquidity-v153-recent.yml"
permissions:
  contents: read
jobs:
  afic-v153-recent:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      V153_RECENT_OUTPUT: artifacts/xau-afic-external-liquidity-v153-recent.json
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install research runtime
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-research-cloud.txt
          pip install pytest==9.0.2 dukascopy-python==4.0.1
      - name: Unit test
        run: pytest -q tests/test_research_xau_afic_external_liquidity_v153.py
      - name: Run V153 recent
        run: python -m fx_scanner.research_xau_afic_external_liquidity_v153_recent_runtime
      - uses: actions/upload-artifact@v4
        with:
          name: xau-afic-external-liquidity-v153-recent
          path: artifacts/xau-afic-external-liquidity-v153-recent.json
          if-no-files-found: error
          retention-days: 30
