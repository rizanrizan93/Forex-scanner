name: XAU V24 Forward Bar Parity V124

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "src/fx_scanner/research_xau_v24_forward_bar_parity_v124.py"
      - "tests/test_research_xau_v24_forward_bar_parity_v124.py"
      - ".github/workflows/research-xau-v24-forward-bar-parity-v124.yml"

permissions:
  contents: read

jobs:
  parity-audit:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      CTRADER_CLIENT_ID: ${{ secrets.CTRADER_CLIENT_ID }}
      CTRADER_CLIENT_SECRET: ${{ secrets.CTRADER_CLIENT_SECRET }}
      CTRADER_ACCESS_TOKEN: ${{ secrets.CTRADER_ACCESS_TOKEN }}
      CTRADER_REFRESH_TOKEN: ${{ secrets.CTRADER_REFRESH_TOKEN }}
      CTRADER_TRADER_LOGIN: ${{ secrets.CTRADER_TRADER_LOGIN }}
      CTRADER_ACCOUNT_ID: ${{ secrets.CTRADER_ACCOUNT_ID }}
      CTRADER_TOKEN_STATE_PATH: /tmp/ctrader_tokens.json
      CTRADER_TOKEN_STATE_DURABLE: "1"
      CTRADER_DISABLE_TOKEN_REFRESH: "1"
      SUPABASE_URL: https://fotzcxjeypmjldhvfskt.supabase.co
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
      V124_PARITY_OUTPUT: artifacts/xau-v24-forward-bar-parity-v124.json

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install research runtime
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-research-cloud.txt
          pip install pytest==9.0.2
          pip install -e .
      - name: Unit tests
        run: pytest -q tests/test_research_xau_v24_forward_bar_parity_v124.py
      - name: Run V124 parity audit
        run: python -m fx_scanner.research_xau_v24_forward_bar_parity_v124
      - uses: actions/upload-artifact@v4
        with:
          name: xau-v24-forward-bar-parity-v124
          path: artifacts/xau-v24-forward-bar-parity-v124.json
          if-no-files-found: error
          retention-days: 30
