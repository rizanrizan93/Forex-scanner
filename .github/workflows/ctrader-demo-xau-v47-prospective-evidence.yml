name: cTrader Demo XAU V47 Prospective Evidence

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/ctrader-demo-xau-v47-prospective-evidence.yml"
  schedule:
    - cron: "*/15 * * * 1-5"

permissions:
  contents: read

jobs:
  validate-scheduler:
    runs-on: ubuntu-latest
    steps:
      - run: echo "xau-v47-prospective-scheduler-syntax-ok"
