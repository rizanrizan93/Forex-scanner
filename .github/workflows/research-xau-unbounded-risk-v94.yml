name: XAU Unbounded Risk V94
on:
  workflow_dispatch:
  pull_request:
    paths:
      - "src/fx_scanner/research_xau_unbounded_risk_v94.py"
      - "src/fx_scanner/research_xau_unbounded_risk_v94_runtime.py"
      - "tests/test_research_xau_unbounded_risk_v94.py"
      - ".github/workflows/research-xau-unbounded-risk-v94.yml"
permissions: {contents: read}
concurrency:
  group: research-xau-unbounded-risk-v94-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    env:
      V94_EVIDENCE_OUTPUT: artifacts/xau-unbounded-risk-v94.json
      SUPABASE_URL: https://fotzcxjeypmjldhvfskt.supabase.co
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.11",cache: pip}
      - name: Install research runtime
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-research-cloud.txt
          pip install pytest==9.0.2 dukascopy-python==4.0.1
      - name: Unit tests
        run: pytest -q tests/test_research_xau_unbounded_risk_v94.py
      - name: Run unbounded-risk diagnostic
        run: python -m fx_scanner.research_xau_unbounded_risk_v94_runtime
      - name: Upload evidence
        uses: actions/upload-artifact@v4
        with:
          name: xau-unbounded-risk-v94
          path: artifacts/xau-unbounded-risk-v94.json
          if-no-files-found: error
          retention-days: 30
