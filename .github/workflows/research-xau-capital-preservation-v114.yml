name: XAU Capital Preservation V114
on:
  workflow_dispatch:
  pull_request:
    paths:
      - "src/fx_scanner/research_xau_capital_preservation_v114.py"
      - "src/fx_scanner/research_xau_capital_preservation_v114_runtime.py"
      - "tests/test_research_xau_capital_preservation_v114.py"
      - ".github/workflows/research-xau-capital-preservation-v114.yml"
permissions: {contents: read}
jobs:
  diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    env:
      V114_EVIDENCE_OUTPUT: artifacts/xau-capital-preservation-v114.json
      SUPABASE_URL: https://fotzcxjeypmjldhvfskt.supabase.co
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.11",cache: pip}
      - name: Install research runtime
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-research-cloud.txt
          pip install pytest==9.0.2 dukascopy-python==4.0.1
      - name: Unit tests
        run: pytest -q tests/test_research_xau_capital_preservation_v114.py
      - name: Run capital preservation diagnostic
        run: python -m fx_scanner.research_xau_capital_preservation_v114_runtime
      - name: Upload evidence
        uses: actions/upload-artifact@v4
        with:
          name: xau-capital-preservation-v114
          path: artifacts/xau-capital-preservation-v114.json
          if-no-files-found: error
          retention-days: 30
