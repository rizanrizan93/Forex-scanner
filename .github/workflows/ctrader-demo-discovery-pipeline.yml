name: cTrader Demo Discovery Pipeline

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ctrader-demo-discovery-pipeline
  cancel-in-progress: false

jobs:
  full-universe-discovery:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      CTRADER_CLIENT_ID: ${{ secrets.CTRADER_CLIENT_ID }}
      CTRADER_CLIENT_SECRET: ${{ secrets.CTRADER_CLIENT_SECRET }}
      CTRADER_ACCESS_TOKEN: ${{ secrets.CTRADER_ACCESS_TOKEN }}
      CTRADER_REFRESH_TOKEN: ${{ secrets.CTRADER_REFRESH_TOKEN }}
      CTRADER_TRADER_LOGIN: ${{ secrets.CTRADER_TRADER_LOGIN }}
      CTRADER_ACCOUNT_ID: ${{ secrets.CTRADER_ACCOUNT_ID }}
      CTRADER_TOKEN_STATE_PATH: /tmp/ctrader_tokens.json
      CTRADER_TOKEN_STATE_DURABLE: "1"
      CTRADER_DISABLE_TOKEN_REFRESH: "1"
      SUPABASE_URL: https://fotzcxjeypmjldhvfskt.supabase.co
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
      FX_KILL_SWITCH: "0"
      CTRADER_DEMO_AUTOTRADE_ENABLED: ${{ secrets.CTRADER_DEMO_AUTOTRADE_ENABLED }}
      CTRADER_DEMO_EXECUTION_CANDIDATE_MIN: "50.01"
      CTRADER_DEMO_TECHNICAL_ONLY: "1"
      CTRADER_DEMO_CALIBRATION_ALLOW_PRETRIGGER: "1"
      CTRADER_DEMO_CHASE_BLOCK_ATR: "2.0"
      CTRADER_DEMO_WAVE_MIN_PULLBACK_ATR: "0.25"
      CTRADER_DEMO_WAVE_MAX_ZONE_ATR: "0.50"
      CTRADER_DEMO_MOMENTUM_MAX_ZONE_ATR: "0.20"
      CTRADER_DEMO_MAX_ENTRY_DRIFT_R: "0.50"
      CTRADER_DEMO_RISK_PER_TRADE_PCT: "1.0"
      CTRADER_DEMO_DEEP_ANALYSIS_TOP: "8"
      CTRADER_DEMO_FVG_MAX_AGE_MINUTES: "90"
      CTRADER_DEMO_XAUUSD_MAX_SPREAD_PIPS: "30"
      CTRADER_DEMO_XTIUSD_MAX_SPREAD_PIPS: "6"
      CTRADER_DEMO_BTCUSD_MAX_SPREAD_PIPS: "20"
      CTRADER_DEMO_ETHUSD_MAX_SPREAD_PIPS: "1"
      CTRADER_DEMO_SOLUSD_MAX_SPREAD_PIPS: "120"
      CTRADER_DEMO_STRUCTURAL_PROFIT_PROTECT_ENABLED: "1"
      CTRADER_DEMO_ADAPTIVE_CALIBRATION_ENABLED: "1"

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
      - name: Install cTrader runtime
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-research-cloud.txt
      - name: Validate fail-closed config
        run: python -m fx_scanner.cli validate-config
      - name: Verify active weekend crypto broker contracts and sessions
        run: python -m fx_scanner.demo_crypto_broker_preflight
      - name: Discover and rank full DEMO universe
        run: |
          # The Python market schedule is UTC-based. On weekends only the three
          # authorized crypto symbols are selected. Weekday-only spread overrides
          # must not be presented to the already-scheduled crypto-only config.
          # Broker trading sessions remain authoritative and fail closed.
          if [ "$(date -u +%u)" -ge 6 ]; then
            export CTRADER_DEMO_XAUUSD_MAX_SPREAD_PIPS=""
            export CTRADER_DEMO_XTIUSD_MAX_SPREAD_PIPS=""
          fi
          python -m fx_scanner.demo_technical_producer
      - name: Reconcile cTrader closed DEMO outcomes
        if: ${{ always() }}
        continue-on-error: true
        run: python -m fx_scanner.demo_closed_trade_reconciler
      - name: Finalize sampled MAE and MFE trajectories
        if: ${{ always() }}
        continue-on-error: true
        run: python -m fx_scanner.demo_trajectory_finalizer
      - name: Summarize floating P&L and incremental calibration
        if: ${{ always() }}
        continue-on-error: true
        run: python -m fx_scanner.demo_incremental_calibration
      - name: Build Adaptive Calibration v2 shadow cohorts and diagnostics
        if: ${{ always() }}
        continue-on-error: true
        run: python -m fx_scanner.demo_adaptive_calibration_v2_runtime
      - name: Attribute repeated loss patterns in shadow mode
        if: ${{ always() }}
        continue-on-error: true
        run: python -m fx_scanner.demo_loss_attribution_v2_runtime
