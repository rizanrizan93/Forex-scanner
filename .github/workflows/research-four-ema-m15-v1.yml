name: Research Four EMA M15 V1

on:
  push:
    branches:
      - research/four-ema-m5-v1
    paths:
      - "src/fx_scanner/research_four_ema_m15_v1.py"
      - "src/fx_scanner/demo_four_ema_historical_research.py"
      - "tests/test_research_four_ema_m15_v1.py"
      - ".github/workflows/research-four-ema-m15-v1.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: research-four-ema-m15-v1
  cancel-in-progress: true

jobs:
  historical-screen:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      CTRADER_CLIENT_ID: ${{ secrets.CTRADER_CLIENT_ID }}
      CTRADER_CLIENT_SECRET: ${{ secrets.CTRADER_CLIENT_SECRET }}
      CTRADER_ACCESS_TOKEN: ${{ secrets.CTRADER_ACCESS_TOKEN }}
      CTRADER_REFRESH_TOKEN: ${{ secrets.CTRADER_REFRESH_TOKEN }}
      CTRADER_TRADER_LOGIN: ${{ secrets.CTRADER_TRADER_LOGIN }}
      CTRADER_ACCOUNT_ID: ${{ secrets.CTRADER_ACCOUNT_ID }}
      CTRADER_TOKEN_STATE_PATH: /tmp/ctrader_tokens.json
      CTRADER_TOKEN_STATE_DURABLE: "1"
      CTRADER_DISABLE_TOKEN_REFRESH: "1"
      SUPABASE_URL: https://fotzcxjeypmjldhvfskt.supabase.co
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-research-cloud.txt
          pip install pytest==9.0.2
      - name: Validate research-only config
        run: python -m fx_scanner.cli validate-config
      - name: Unit tests
        run: pytest -q tests/test_research_four_ema_m15_v1.py
      - name: Run cTrader historical screen
        shell: bash
        run: |
          set -euo pipefail
          python -m fx_scanner.demo_four_ema_historical_research | tee forex_four_ema_m15_v1.json
      - name: Print evidence
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          data = json.loads(Path("forex_four_ema_m15_v1.json").read_text())
          assert data["research_only"] is True
          assert data["execution_influence"] is False
          assert data["oos_used_for_selection"] is False
          print("FOUR_EMA_SCREEN_PASS", data["screen_pass"])
          for row in data["rows"]:
              v = row["stress"]["validation"]
              o = row["stress"]["oos"]
              print(
                  "FOUR_EMA_ROW",
                  row["symbol"], row["candidate"],
                  "validation_n=", v["trades"],
                  "validation_wr=", v["win_rate"],
                  "validation_exp=", v["expectancy_r"],
                  "validation_pf=", v["profit_factor"],
                  "oos_n=", o["trades"],
                  "oos_wr=", o["win_rate"],
                  "oos_exp=", o["expectancy_r"],
                  "oos_pf=", o["profit_factor"],
                  "oos_dd=", o["max_drawdown_r"],
                  "pass=", row["screen_pass"],
              )
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: forex-four-ema-m15-v1
          path: forex_four_ema_m15_v1.json
          if-no-files-found: error
