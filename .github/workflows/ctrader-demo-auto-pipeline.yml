name: cTrader Demo Auto Pipeline

on:
  workflow_dispatch:
  schedule:
    # Explicit fixed-minute slots avoid step/range scheduler registration issues.
    # Still exactly every 5 minutes on weekdays, offset from minute 0.
    - cron: "2,7,12,17,22,27,32,37,42,47,52,57 * * * 1-5"

permissions:
  contents: read

concurrency:
  group: ctrader-demo-auto-pipeline
  cancel-in-progress: false

jobs:
  scan-and-execute:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      CTRADER_CLIENT_ID: ${{ secrets.CTRADER_CLIENT_ID }}
      CTRADER_CLIENT_SECRET: ${{ secrets.CTRADER_CLIENT_SECRET }}
      CTRADER_ACCESS_TOKEN: ${{ secrets.CTRADER_ACCESS_TOKEN }}
      CTRADER_REFRESH_TOKEN: ${{ secrets.CTRADER_REFRESH_TOKEN }}
      CTRADER_TRADER_LOGIN: ${{ secrets.CTRADER_TRADER_LOGIN }}
      CTRADER_ACCOUNT_ID: ${{ secrets.CTRADER_ACCOUNT_ID }}
      CTRADER_TOKEN_STATE_PATH: /tmp/ctrader_tokens.json
      CTRADER_DISABLE_TOKEN_REFRESH: "1"
      SUPABASE_URL: https://fotzcxjeypmjldhvfskt.supabase.co
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
      FX_KILL_SWITCH: "0"
      FX_IDEMPOTENCY_STATE_PATH: /tmp/execution_idempotency.json
      CTRADER_DEMO_AUTOTRADE_ENABLED: ${{ secrets.CTRADER_DEMO_AUTOTRADE_ENABLED }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
      - name: Install cTrader runtime
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-research-cloud.txt
      - name: Validate fail-closed config
        run: python -m fx_scanner.cli validate-config
      - name: Produce durable DEMO-only signals
        run: python -m fx_scanner.cli ctrader-signal-producer
      - name: Execute only durable EXECUTION_READY DEMO signals
        run: python -m fx_scanner.cli ctrader-demo-autotrade --once --limit 10
