name: Research Session Breakout USDJPY V1.1

on:
  pull_request:
    paths:
      - "research/asia_london_breakout_v1.py"
      - "research/asia_london_breakout_v1_1.py"
      - ".github/workflows/research-session-breakout-usdjpy-v1-1.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
      - name: Install pinned research dependencies
        run: pip install pandas==2.2.3 numpy==2.1.3 dukascopy-python==4.0.1
      - name: Run frozen USDJPY rule with index-only correction
        run: |
          mkdir -p research_output_session_v1
          python - <<'PY' | tee research_output_session_v1/console-usdjpy-v1-1.txt
          import sys
          sys.path.insert(0, 'research')
          import asia_london_breakout_v1 as base
          import asia_london_breakout_v1_1 as fixed
          usd = base.PAIR_INSTRUMENTS['USDJPY']
          base.PAIR_INSTRUMENTS = {'USDJPY': usd}
          fixed.main()
          PY
      - name: Verify corrected research-only contract
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          d = json.loads(Path('research_output_session_v1/asia_london_breakout_v1.json').read_text())
          assert d['contract'] == 'ASIA_LONDON_BREAKOUT_V1_1_INDEX_FIX'
          assert d['execution_influence'] is False
          assert list(d['coverage']) == ['USDJPY']
          assert d['coverage']['USDJPY']['rows'] >= 150000
          print('USDJPY V1.1 index-only correction: PASS')
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: asia-london-breakout-usdjpy-v1-1-results
          path: research_output_session_v1/
          retention-days: 14
